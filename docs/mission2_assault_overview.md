# 미션 2 주공세 정리

> 기준 파일: `Triggers/18_mission2_main_assaults_*`
> 보조 파일: `Triggers/03_master_tick.txt`, `Triggers/18a_mission2_hive_cooldown_penalty.txt`, `Triggers/18b_mission2_p6_minibase.txt`
> 시간 기준: 12 DC tick = 1초

이 문서는 현재 미션 2의 **P6/P7 잔존 군락 주공세**만 정리한다. 자원 습격, 9분 공습, 미션 1 웨이브, 별도 테스트 트리거는 포함하지 않는다.

---

## 1. 역할 요약

미션 2 주공세는 동북쪽 `Player 6` 갈색 저그와 동남쪽 `Player 7` 하얀색 저그가 각각 독립적으로 굴리는 대규모 압박이다.

- P6 주공세 쿨다운: `Player 8`의 `Zerg Zergling` DC
- P7 주공세 쿨다운: `Player 8`의 `Zerg Hydralisk` DC
- P6 진행 상태: `Zerg Lurker`, `Zerg Ultralisk`, `Zerg Scourge`, `Protoss Reaver`
- P7 진행 상태: `Zerg Mutalisk`, `Zerg Devourer`, `Zerg Guardian`, `Protoss Observer`
- P6 랜덤 웨이브: `Switch9/10`
- P7 랜덤 웨이브: `Switch11/12`
- P6 랜덤 경유지: `Switch14/15/16`
- P7 랜덤 경유지: `Switch17/18/19`
- P6 랜덤 최종 목표: `Switch20/21`
- P7 랜덤 최종 목표: `Switch22/23`

주공세는 생성 직후 바로 플레이어 본진으로 직행하지 않는다. 먼저 `Waypoint1~5` 중 하나로 랠리한 뒤, 60초 동안 주기적으로 선택된 `P1~P4 Hunt` 위치로 재명령된다. 선택된 플레이어가 사망하면 타깃 보정 트리거가 살아 있는 플레이어 쪽으로 다시 돌린다.

---

## 2. 첫 공세 타이밍

미션 2 진입 직후 P6/P7 각 군락의 쿨다운 지터를 독립적으로 섞는다. 첫 공세와 이후 반복 공세는 같은 쿨다운 밴드를 사용하며, 쿨다운은 미션 2 진입(`Valkyrie = 3`) 직후부터 전진한다.

| 난이도 | 접촉 경보 | 실제 스폰 | 설명 |
|------|------|-----------|------|
| 쉬움 | `32분 15초` / `33분` / `33분 45초` | 접촉 경보 +10초 | 기본 쿨다운 밴드 |
| 보통 | `26분 15초` / `27분` / `27분 45초` | 접촉 경보 +10초 | 난이도 보정으로 첫 사이클부터 앞당김 |
| 어려움 | `23분 15초` / `24분` / `24분 45초` | 접촉 경보 +10초 | 난이도 보정으로 첫 사이클부터 앞당김 |
| 매우 어려움 | `20분 15초` / `21분` / `21분 45초` | 접촉 경보 +10초 | 난이도 보정으로 첫 사이클부터 앞당김 |

P6/P7은 서로 다른 지터 스위치를 사용하므로 첫 공세부터 각 군락이 독립적으로 움직인다. 보통 난이도 기준 첫 접촉 경보는 미션2 시작 후 `26분 15초` / `27분` / `27분 45초` 중 하나다.

쿨다운은 미션 2에서만 기본 증가하지만, 미션 1 중 외곽 군락을 건드려 깨우면 해당 군락의 쿨다운만 미션 1부터 먼저 전진한다.

### 웨이브 사전 확정

- **첫 공세**: 게임 시작 시 `Switch132` 1회 트리거로 P6 `Switch61/62`, P7 `Switch65/66`를 randomize. 미션 1 패널티 공세·테스트에도 동일하게 적용된다.
- **이후 공세**: consume(접촉 경보) 직후 같은 스위치를 다시 randomize해 다음 사이클 웨이브를 즉시 확정한다.
- **스폰용 복사**: detailed-analysis 시점(spawn-90s)에 `Switch61/62` → `Switch9/10`(P7는 `Switch11/12`)로 복사한다. 스폰 파일은 `Switch9/10`만 읽는다.

### 2단계 순차 경고 (실제 스폰 기준)

플레이어에게 표시되는 경고는 상세 분석·접촉 경보 **2단계**다. 알림 피로도를 줄이기 위해 기존 1단계(큰 분류, spawn−180s) 표시는 제거했다. 단, 큰 분류 roll(`Switch128/130`)·cue 펄스(`Switch129/131` shown)는 **무음 내부 앵커**로 그대로 유지된다 — 상세 분석 앵커가 `Switch129/131` Set을 전제로 발화하기 때문이다.

| 단계 | 시점 | 스위치/DC | 내용 |
|------|------|-----------|------|
| (무음 앵커) 큰 분류 | spawn − 180s | `Switch128/130` roll, `Switch129/131` shown | 표시 없음. 상세 분석 발화용 내부 앵커 |
| 1. 상세 분석 | spawn − 90s | `Switch63/67` active, `Switch64/68` shown | 군락 색상 + 4종 웨이브별 노바 분석 |
| 2. 접촉 경보 | spawn − 10s | `Lurker/Mutalisk = 3` | 군락 색상 + 대규모 공세 접근 + 전술 힌트 |

모든 경고 메시지는 P6/P7 트리거가 분리되어 있으며, `<15>갈색 저그` / `<16>하얀색 저그` 색 코드로 출처 군락을 표시한다. 미니맵 핑은 `P6/P7 Spawn1~4`.

쿨다운 가속으로 임계값을 건너뛰면 `At least` 조건 덕에 해당 경고가 즉시 발화한다(리드타임만 단축, 누락 없음).

---

## 3. 쿨다운 가속

미션 2에서 P6/P7의 `Hatchery`, `Lair`, `Hive` 처치 기록이 들어오면 해당 군락의 다음 주공세 쿨다운이 난이도별로 앞당겨지고, 동시에 자원 보상과 생산망 손실 카운터(`Zerg Sunken/Spore Colony`)가 누적된다. 첫 주공세 여부와 무관하게 동작한다.

| 난이도 | 해당 군락 가속 | 반대 군락 가속 | tick |
|--------|----------------|----------------|------|
| 쉬움 | 198초 | 없음 | +2376 |
| 보통 | 371.25초 | 없음 | +4455 |
| 어려움 | 382.5초 | 191.25초 | +4590 / +2295 |
| 매우 어려움 | 427.5초 | 427.5초 | +5130 / +5130 |

| 파괴 대상 | 플레이어 보상 | 쿨다운 변화 |
|-----------|---------------|-------------|
| Hatchery | <1F>미네랄 +550  <07>가스 +60<04> | 해당 군락 주공세, 어려움 이상은 반대 군락도 위 표만큼 |
| Lair | <1F>미네랄 +650  <07>가스 +60<04> | 해당 군락 주공세, 어려움 이상은 반대 군락도 위 표만큼 |
| Hive | <1F>미네랄 +750  <07>가스 +60<04> | 해당 군락 주공세, 어려움 이상은 반대 군락도 위 표만큼 |

쿨다운을 소비할 때는 필요한 값만 빼고 초과분을 남긴다. 그래서 건물 파괴가 여러 번 누적되면 다음 공세 뒤에도 일부 진행도가 이어질 수 있다.

**한쪽 군락이 클리어된 뒤에는 가속이 적용되지 않는다.** 한 군락의 핵심 테크가 무너지면(`Switch31`=P6 / `Switch32`=P7), 남은 반대쪽 군락의 `Hatchery`/`Lair`/`Hive`를 파괴해도 쿨다운이 앞당겨지지 않는다 — 자원 보상과 테크 손실 카운터(`Zerg Sunken/Spore Colony` → 4-1의 "최후의 비명")는 그대로 유지된다. 이미 승기를 잡은 상황의 무의미한 소모전을 줄여 게임을 빨리 끝내기 위함이다. (구현: 18a의 가속 트리거에 반대쪽 군락 생존 조건을 달고, 클리어 후에는 가속만 뺀 변형 트리거가 보상·카운터를 처리.)

---

## 4. 일반 주공세 웨이브

P6와 P7은 같은 네 가지 웨이브 풀을 사용한다. 차이는 소유 플레이어와 스폰 위치뿐이다.

아래 **총 스폰**은 **쉬움(베이스) 기준**이다. 보통/어려움/매우 어려움은 `18_..._09_difficulty_wave_scaling.txt`의 쉬움 보존 레이어와 기존 난이도 레이어가 베이스 위에 누적된다. 쉬움은 전투/지원 유닛 기준 직전 쉬움 대비 −30%이며, 보통 이상은 보존 레이어로 이전 총 물량을 유지한다.

| 웨이브 | 선택 조건 | 콘셉트 | 총 스폰(쉬움) |
|--------|-----------|--------|---------|
| 0 | 두 스위치 모두 `Not Set` | 울트라 + 저글링 + 히드라 지상 돌파 | 저글링 95, 울트라 11, 히드라 8, 디파일러 4, 오버로드 4 |
| 1 | 첫 스위치 `Set`, 둘째 `Not Set` | 스커지 + 디바우러 + 뮤탈 + 퀸 공중 압박 | 스커지 29, 디바우러 14, 뮤탈 18, 퀸 1, 오버로드 4 |
| 2 | 첫 스위치 `Not Set`, 둘째 `Set` | 히드라 대량 압박 | 히드라 95, 디바우러 4, 오버로드 4 |
| 3 | 두 스위치 모두 `Set` | 뮤탈 + 가디언 화력 압박 | 뮤탈 25, 가디언 15, 오버로드 4 |

분할 스폰 구조:

- 웨이브 0은 한 번에 4개 스폰 지점에서 생성된다.
- 웨이브 1은 3개 배치로 나뉘며, 약 0초 / 3초 / 6초 간격으로 생성된다.
- 웨이브 2는 5개 배치로 나뉘며, 약 0초 / 3초 / 6초 / 9초 / 12초 간격으로 생성된다.
- 웨이브 3은 3개 배치로 나뉘며, 약 0초 / 3초 / 6초 간격으로 생성된다.

각 배치가 스폰될 때 `Switch53` 또는 `Switch54`가 한 트리거 사이클 열리고, 랜덤 경유지 랠리 파일이 개별 유닛 타입별 `Order`를 실행한다. `Men` 단위 명령은 쓰지 않는다.

---

## 5. 경유지와 최종 목표

주공세는 두 단계로 이동한다.

1. 스폰 직후 `Waypoint1~5` 중 하나로 랠리한다.
2. 마지막 스폰 배치 이후 60초 동안 선택된 `P1~P4 Hunt` 위치로 재명령한다.

최종 목표 재명령은 계속 매 프레임 난사하지 않는다. 60초 창 안에서 제한된 구간에만 릴레이 펄스를 열어 경로 재계산 부담을 줄인다.

---

## 6. 군락 붕괴 처리

한쪽 군락의 `P6 MainBase` 또는 `P7 MainBase`에 `Hatchery/Lair/Hive`가 모두 사라지면 해당 군락의 주공세는 잠긴다.

- 해당 군락의 쿨다운은 0으로 정리된다.
- 경보 상태 또는 스폰 대기 상태였던 공세는 취소된다.
- 한쪽 군락만 먼저 무너졌고 반대쪽 군락이 살아 있으면 모든 플레이어에게 <1F>미네랄 +2000<04>, <07>가스 +1000<04> 보상이 지급된다.
- 두 군락이 모두 무너지면 미션 2 클리어 흐름으로 넘어간다.

---

## 7. 오버마인드 고치 약화 이벤트

미션 2 약 12분(8640 ticks)에 `Overmind Cocoon` 약화 이벤트가 열린다.

- 양쪽 군락이 모두 살아 있으면 P6/P7 중 하나를 선택한다. **쉬움(`Terran Academy = 1`)은 항상 P6**, 그 외 난이도는 랜덤.
- 한쪽 군락이 이미 무너졌으면 살아 있는 군락 쪽 고치만 선택한다.
- 선택된 고치는 무적이 해제되고 미니맵/시야로 표시된다.
- 파괴 보상은 모든 플레이어에게 <1F>미네랄 +500<04>, <07>가스 +250<04>이다.
- 파괴에 성공하면 선택된 군락의 다음 주공세 1회가 약화된다.

약화된 주공세는 일반 네 가지 웨이브를 굴리지 않고, 총 저글링 70기만 생성한 뒤 동일한 경유지/최종 목표 릴레이를 사용한다.

---

## 8. P6 전방 집결지 연동

미션2 시작 후 5:40(4080 ticks, `Terran Vulture` DC)에 `P6 MiniBase`가 `전방 집결지`라는 이름으로 공개된다.

- 표시 조건: 미션2 경과 5:40, `P6 MiniBase`에 Hatchery/Lair/Hive 존재
- 표시 효과: P1~P4 Map Revealer, `P6 MiniBase` 미니맵 핑, 노바/호크 안내
- 보상 조건: `P6 MiniBase`의 Hatchery/Lair/Hive 제거
- 보상: 모든 플레이어에게 <1F>미네랄 +1000<04>, <07>가스 +500
- 전술 효과: P6 다음 주공세 쿨다운 진행도 2880 ticks 차감, 즉 최대 4분 지연

이 이벤트는 플레이어가 미션2 초반에 갈라지지 않고 동북쪽 갈색 군락으로 모이게 하는 소프트 가이드다. P7은 여전히 먼저 파괴 가능하지만, 시작 시 시야/핑을 주지 않아 초반 정보 우선순위에서는 뒤로 밀린다.

P5 주황색 군락 방어기관은 미션2 시작 후 2분 20초(1680 ticks)에 점막 뿌리망 고사와 함께 제거된다.

---

## 9. 능동 공세 유도 안내

플레이어가 방어 소모전에 갇히지 않고 "유효타 → 반격 격퇴 → 다음 유효타" 루프를 타도록 돕는 메시지 계층. 시스템 수치를 바꾸지 않으며, 기존 사이클을 보이게 만드는 역할만 한다.

### 공격 창 안내 (`18e_mission2_offense_window.txt`)

각 군락의 주공세 스폰 시작 3분(2160틱) 후 — 웨이브가 방어선에서 정리될 즈음 — "군락이 비어 있는 지금이 진격 기회"를 알린다. 매 사이클 반복하며, 호크/노바 4종 대사 바리에이션을 무작위로 선택해 피로를 줄인다. 난이도·지터·가속에 따라 달라지는 구체적 시간 수치는 언급하지 않는다.

- 타이머 DC: P6 = P8 `Terran SCV`, P7 = P8 `Terran Missile Turret` (상태 플래그 spawning 진입 시 가동)
- 바리에이션 스위치: P6 = `Switch70/155`, P7 = `Switch156/157`
- 억제 조건: 해당 군락 붕괴(`Switch31/32`), 최후의 비명 활성(`Switch74/75`)
- 첫 발화: 첫 군락 ≈ 미션2 7:43, 둘째 군락 ≈ 11:13

### 생산망 마일스톤 (`18h_mission2_colony_progress.txt`)

18a의 군락별 Hatchery/Lair/Hive 누적 손실 카운터(P8 `Zerg Sunken/Spore Colony`)가 5 / 10 / 15에 도달할 때 1회성 진행 보고(균열 → 절반 붕괴 → 한계)를 띄운다. 기준은 군락당 해처리 기본 15 + 난이도 보너스 1/2/3 + AI 멀티 최대 3.

### 메시지 재프레이밍

- 18a의 첫 가속 안내를 위협 프레임에서 거래 프레임("반격을 막아낼수록 군락은 약해진 채 다음 타격을 받습니다")으로 교체.
- 오버마인드 고치 공개 시퀀스(00b)에 "약화된 공세 직후가 진격 기회" 노바 교리 라인 추가, 기존 한 트리거에 겹쳐 있던 호크/작전 정보 2줄을 84틱 간격 별도 트리거로 분리.

## 10. 파일 책임

| 파일 | 책임 |
|------|------|
| `18_mission2_main_assaults_00_init_and_colony_state.txt` | 미션 2 진입 초기화, 게임 시작 웨이브 시드, 첫 공세 순서, 군락 붕괴/보상 |
| `18_mission2_main_assaults_00a_difficulty_timing.txt` | 난이도별 쿨다운 밴드(큰 분류 무음 앵커 + 2단계 경고 타이밍 유지) |
| `18_mission2_main_assaults_00b_overmind_cocoon.txt` | 오버마인드 고치 이벤트와 약화 공세 |
| `18b_mission2_p6_minibase.txt` | P6 전방 집결지 공개/보상 |
| `18_mission2_main_assaults_01_p6_rolls.txt` | P6 coarse/detailed roll, wave copy, consume |
| `18_mission2_main_assaults_02_p7_rolls.txt` | P7 coarse/detailed roll, wave copy, consume |
| `18_mission2_main_assaults_03_warnings.txt` | 2단계 경고(상세/접촉) 메시지 + 큰 분류 무음 cue 펄스 |
| `18_mission2_main_assaults_04_p6_target_correction.txt` | P6 목표 플레이어 사망 보정 |
| `18_mission2_main_assaults_05_p7_target_correction.txt` | P7 목표 플레이어 사망 보정 |
| `18_mission2_main_assaults_06_spawn_delay.txt` | 일반 공세 10초 경보 후 스폰 전환 |
| `18_mission2_main_assaults_10~13_*` | P6 일반 웨이브 0~3 |
| `18_mission2_main_assaults_20~23_*` | P7 일반 웨이브 0~3 |
| `18_mission2_main_assaults_30_p6_random_rally.txt` | P6 스폰 직후 랜덤 경유지 랠리 |
| `18_mission2_main_assaults_31_p7_random_rally.txt` | P7 스폰 직후 랜덤 경유지 랠리 |
| `18_mission2_main_assaults_40_p6_hunt_relay.txt` | P6 경유지에서 플레이어 Hunt 위치로 재명령 |
| `18_mission2_main_assaults_41_p7_hunt_relay.txt` | P7 경유지에서 플레이어 Hunt 위치로 재명령 |
| `18e_mission2_offense_window.txt` | 공세 스폰 3분 후 공격 창 안내 (무작위 대사 4종) |
| `18h_mission2_colony_progress.txt` | 군락 생산망 누적 손실 5/10/15 마일스톤 보고 |

---

## 11. 현재 밸런스 포인트

- 가장 큰 지상 물량은 웨이브 0의 저글링 95 + 울트라 11 + 히드라 8이다(쉬움 기준).
- 가장 큰 단일 병종 압박은 웨이브 2의 히드라 95이다(쉬움 기준).
- 가장 위험한 공중 화력은 웨이브 3의 가디언 15 + 뮤탈 25이다(쉬움 기준).
- 오버마인드 고치 파괴에 성공하면 다음 주공세가 저글링 70기로 크게 약화되어, 플레이어가 능동적으로 숨 돌릴 창을 만들 수 있다.
