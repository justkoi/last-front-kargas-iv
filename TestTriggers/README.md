# TestTriggers

Put inactive or archived test triggers here.

Files in this folder are not included by `build_triggers_withTest.ps1`.
Move a test trigger to `TestTriggersForBuild/` when it should be included in the test build.
